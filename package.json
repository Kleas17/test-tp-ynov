{
    "devDependencies": {
        "jest": "^30.2.0"
    },
    "scripts": {
        "test": "jest --coverage --ci --runInBand --testPathIgnorePatterns=my-app",
        "docs": "jsdoc -c jsdoc.json"
    }
}
